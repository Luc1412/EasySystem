[project]
name = "easysystem"
version = "2.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
    "red-discordbot==3.5.24",
]

[tool.ruff.lint]
select = [        
    "E", # pycodestyle
    "F", # Pyflakes
    "UP", # pyupgrade
    "B", # flake8-bugbear
    "SIM", # flake8-simplify
    "I", # isort
    "FAST", # fastapi
]
ignore = [
    "E501", # line too long
]

[dependency-groups]
dev = [
    "ruff>=0.16.0",
]
